package com.example.myapplication.Model;

/**
 * a enum that represent the type of workers(basic, manager) that required for a shift
 */
public enum RoleLevel {
    BASIC,
    MANAGER,
    SUPER_ADMIN
}
